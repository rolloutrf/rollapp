# Local development uses an in-memory demo store when DATABASE_URL is omitted.
# DATABASE_URL=postgresql://rollwish:password@host:6432/rollwish?sslmode=verify-full
PORT=8080
APP_ORIGIN=http://localhost:5173
SESSION_SECRET=replace-with-at-least-32-random-characters
DEMO_MODE=true
